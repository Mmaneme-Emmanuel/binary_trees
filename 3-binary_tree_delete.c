#include <binary_trees.h>
#include "stdlib.h"

/**
 * binary_tree_delete - function that deletes an entire binary tree
 * @tree: the tree of nodes to be deleted
 */
void binary_tree_delete(binary_tree_t *tree)
{
	if (tree == NULL)
		return;
}
